#include <stdio.h>

int main() {
    int x;
    scanf("%d",&x);
    if (3 > x){
        printf("GOLD\n");
    }
    else if(3 <= x && x < 6){
        printf("SILVER\n");
    }
     else if(6 <= x ){
        printf("BRONZE\n");
    }
    return 0;
}

