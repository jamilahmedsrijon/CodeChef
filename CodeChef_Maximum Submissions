#include <stdio.h>

int main() {
    int t,x;
    scanf("%d",&t);
    for (int i = 0; i<t;i++){
        scanf("%d",&x);
        int time = x * 60;
        int res = time / 30;
        printf("%d\n",res);
    }

return 0;
}

