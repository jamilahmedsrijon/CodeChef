#include<stdio.h>
int main ()
{
    int t,x,y,i,res;
    scanf("%d",&t);
    for(i = 0; i < t; i++){
        scanf("%d",&x);
        res = x * 4;
        if (res <= 1000 ) printf("YES\n");

        else  printf("NO\n");

    }
    return 0;
}
