#include<stdio.h>

int main()
{
    int t,p,b,i;
    scanf("%d",&t);
   for (i = 0; i<t;i++){
        scanf("%d%d",&p,&b);
      if(p<=b){
            printf("%d\n",p);
      }
      else if (p>=b) {
         printf("%d\n",b);
      }

    }

    return 0;
}
