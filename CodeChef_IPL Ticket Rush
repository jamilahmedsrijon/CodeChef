#include<stdio.h>

int main()
{
    int t,x,n,i,min;
    scanf("%d",&t);
   for (i = 0; i<t;i++){
        scanf("%d%d",&x,&n);
        min = x - n;
            if (min <  0){
            printf("0\n");
        }
        else printf("%d\n",min);

   }
    return 0;
}
