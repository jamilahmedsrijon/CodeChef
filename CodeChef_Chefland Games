#include <stdio.h>

int main(void) 
{
    int t,a,b,c,e,i;
    
    scanf("%d",&t);
    
    for (i = 0; i<t; i++)
    {
        scanf("%d %d %d %d",&a,&b,&c,&e);
        
        if(a == 0 && b ==0 && c == 0 && e == 0)
        {
            printf("IN\n");
        }
        
        else  printf("OUT\n");
    }

return 0;


}
