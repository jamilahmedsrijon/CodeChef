#include <stdio.h>

int main() 
{
    int t,i,x;
    scanf("%d",&t);
    
    for (i = 0; i < t; i++)
    {
        scanf("%d",&x);
        
        if (x == 1 || x == 2 || x == 3 || x == 4  )
            printf("YES\n"); 
        
        else printf("NO\n");
    }
    return 0;

}

